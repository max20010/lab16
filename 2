#include "pch.h"
#include <iostream>
using namespace std;
int main()
{
	int n,n1, x = 0, k = 0,num=0;
	cin >> n;
	int* m = new int[n];
	for (int i = 0; i < n; i++) {
		cin >> m[i];
	}
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			if (i != j && m[i] == m[j]) {
				x++;
			}
		}
		if (x == 1) {
			k++;
		}
		x = 0;
	}
	n1 = n - k;
	int* m1 = new int[n1];
	for (int i = 0; i < n; i++) {
		for (int j = 0; j < n; j++) {
			if (i != j && m[i] == m[j]) {
				x++;
			}
		}
		if (x != 1) {
			m1[num] = m[i];
			num++;
		}
		x = 0;
	}
	cout << endl;

	for (int i = 0; i < n1; i++) {
		cout << m1[i] << endl;
	}
	delete[]m;
	delete[]m1;
}
