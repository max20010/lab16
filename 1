
#include "pch.h"
#include <iostream>
using namespace std;

int main()
{
	int n,x=0;
	cin >> n;
	int* m = new int[n];
	for (int i = 0; i < n; i++) {
		cin >> m[i];
	}
	for (int i = 1; i < n; i++) {
		if (m[i] == m[i - 1]) {
			x++;
			i++;
		}
	}
	x = n - x;
	int* m1 = new int [x];
	for (int i = 0, j = 0; i < x; i++, j++) {
		if (m[j] == m[j - 1]) {
			
			j++;
		}
		m1[i] = m[j];
	}
	cout << endl;
	for (int i = 0; i < x; i++) {
		cout << m1[i] << endl;
	}
	delete[]m;
	delete[]m1;
}

